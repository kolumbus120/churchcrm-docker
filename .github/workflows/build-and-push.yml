name: Build and Push Docker Image

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - '.github/workflows/build-and-push.yml'
      - 'DOCKERHUB_OVERVIEW.md'
  workflow_dispatch:
    inputs:
      force_latest:
        description: 'Force update :latest (use to promote a major version)'
        type: boolean
        default: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get latest ChurchCRM version
        id: version
        run: |
          LATEST=$(curl -s https://api.github.com/repos/ChurchCRM/CRM/releases/latest | jq -r '.tag_name' | sed 's/^v//')
          MAJOR=$(echo "$LATEST" | cut -d. -f1)
          echo "version=$LATEST" >> $GITHUB_OUTPUT
          echo "major=$MAJOR" >> $GITHUB_OUTPUT
          echo "prev_major=$((MAJOR - 1))" >> $GITHUB_OUTPUT

      - name: Check build conditions
        id: check
        run: |
          MAJOR="${{ steps.version.outputs.major }}"
          PREV="${{ steps.version.outputs.prev_major }}"
          HUB="https://hub.docker.com/v2/repositories/${{ secrets.DOCKER_HUB_USER }}/churchcrm/tags"

          M_HTTP=$(curl -s -o /dev/null -w "%{http_code}" "$HUB/$MAJOR/")
          P_HTTP=$(curl -s -o /dev/null -w "%{http_code}" "$HUB/$PREV/")

          if [ "$M_HTTP" != "200" ] && [ "$P_HTTP" = "200" ]; then
            echo "major_jump=true" >> $GITHUB_OUTPUT
            echo "Major version jump detected: $PREV to $MAJOR"
          else
            echo "major_jump=false" >> $GITHUB_OUTPUT
            echo "Building version ${{ steps.version.outputs.version }}"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USER }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and push (normal update or force_latest)
        if: steps.check.outputs.major_jump != 'true' || inputs.force_latest == true
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: CHURCHCRM_VERSION=${{ steps.version.outputs.version }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:latest
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:${{ steps.version.outputs.major }}
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:${{ steps.version.outputs.version }}

      - name: Build and push (major jump - :latest protected)
        if: steps.check.outputs.major_jump == 'true' && inputs.force_latest != true
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: CHURCHCRM_VERSION=${{ steps.version.outputs.version }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:${{ steps.version.outputs.major }}
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:${{ steps.version.outputs.version }}

      - name: Create GitHub issue for major version jump
        if: |
          steps.check.outputs.major_jump == 'true' &&
          inputs.force_latest != true
        uses: actions/github-script@v7
        with:
          script: |
            const version = '${{ steps.version.outputs.version }}';
            const major = '${{ steps.version.outputs.major }}';
            const prev = '${{ steps.version.outputs.prev_major }}';
            const user = '${{ secrets.DOCKER_HUB_USER }}';
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Major version available: ChurchCRM ${version}`,
              body: [
                `## ChurchCRM ${version} is available`,
                ``,
                `**:latest tag was NOT updated** - it stays on the last ${prev}.x version.`,
                ``,
                `New tags published:`,
                `- \`${user}/churchcrm:${version}\``,
                `- \`${user}/churchcrm:${major}\` (tracks all ${major}.x updates)`,
                ``,
                `## Before upgrading`,
                ``,
                `- [ ] Read the [ChurchCRM ${version} release notes](https://github.com/ChurchCRM/CRM/releases/tag/${version})`,
                `- [ ] Backup your database`,
                `- [ ] Test on staging first`,
                ``,
                `## How to upgrade`,
                ``,
                `\`\`\`yaml`,
                `image: ${user}/churchcrm:${version}`,
                `\`\`\``,
                ``,
                `Or pin to the major tag for automatic ${major}.x updates:`,
                ``,
                `\`\`\`yaml`,
                `image: ${user}/churchcrm:${major}`,
                `\`\`\``,
                ``,
                `To promote to \`:latest\`, run the workflow manually with **Force update :latest** checked.`,
              ].join('\n')
            });

  update-dockerhub-description:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update DockerHub description
        run: |
          TOKEN=$(curl -s -X POST "https://hub.docker.com/v2/users/login" \
            -H "Content-Type: application/json" \
            -d '{"username": "${{ secrets.DOCKER_HUB_USER }}", "password": "${{ secrets.DOCKER_HUB_TOKEN }}"}' \
            | jq -r '.token')
          curl -s -X PATCH "https://hub.docker.com/v2/repositories/${{ secrets.DOCKER_HUB_USER }}/churchcrm/" \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"full_description\": $(jq -Rs . < DOCKERHUB_OVERVIEW.md)}"
