name: Build and Push Docker Image

on:
  schedule:
    - cron: '0 2 * * *'  # Každý deň o 2:00 UTC (automatická kontrola)
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - '.gitea/workflows/build-and-push.yml'
  workflow_dispatch:  # Manuálny trigger cez Gitea UI

jobs:
  build:
    runs-on: ubuntu
    steps:
      # 1. Checkout repozitár
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Nastavenie Docker Buildx (pre multi-platform build)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 3. Prihlásenie do Gitea Registry
      - name: Login to Gitea Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.REGISTRY_URL }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      # 4. Prihlásenie do Docker Hub (pre komunitu)
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USER }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      # 5. Získanie najnovšej verzie ChurchCRM z GitHub
      - name: Get latest ChurchCRM version
        id: churchcrm_version
        run: |
          LATEST_VERSION=$(curl -s https://api.github.com/repos/ChurchCRM/CRM/releases/latest | jq -r '.tag_name' | sed 's/^v//')
          echo "Latest ChurchCRM version: $LATEST_VERSION"
          echo "version=$LATEST_VERSION" >> $GITHUB_OUTPUT

      # 6. Získanie aktuálneho dátumu pre tag
      - name: Get current date
        id: date
        run: |
          DATE=$(date +'%Y-%m-%d')
          echo "date=$DATE" >> $GITHUB_OUTPUT

      # 7. Build a push Docker image
      - name: Build and push Docker images
        uses: docker/build-push-action@v4
        with:
          context: .
          build-args: |
            CHURCHCRM_VERSION=${{ steps.churchcrm_version.outputs.version }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ secrets.REGISTRY_URL }}/${{ secrets.REGISTRY_USER }}/churchcrm-docker:latest
            ${{ secrets.REGISTRY_URL }}/${{ secrets.REGISTRY_USER }}/churchcrm-docker:${{ steps.churchcrm_version.outputs.version }}
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:latest
            ${{ secrets.DOCKER_HUB_USER }}/churchcrm:${{ steps.churchcrm_version.outputs.version }}
          # ODSTRÁNENÁ CACHE PRE STABILITU
